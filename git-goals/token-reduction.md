# Git Branch Goal: token-reduction

implement token-reduction feature: content-addressed object store for huge raw outputs (git-annex/lfs style) + thin git-tracked summary records + pluggable filters borrowed from TokenReduction tools
