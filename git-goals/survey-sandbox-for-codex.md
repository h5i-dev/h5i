# Git Branch Goal: survey-sandbox-for-codex

Linus-review punch list: (1) adversarial sandbox tests, (2) decompose fn main, (3) cargo workspace + feature gates, (4) pub(crate) surface, (5) untangle sandbox policy/dispatch + kill sandbox->msg leak, (6) ref-merge property tests
