{"git_oid":"39503634b8bfcddf7fd42d02890ade28a42605f2","parent_oid":"bed75ec2260baa0d367222992f49f506dbd13d0f","ai_metadata":{"model_name":"claude-opus-4-8","prompt":"Can we make the prompt scoring mechanism faster? Is it already maximaly optimized?\n\n<task-notification>\n<task-id>a2517b77a93d6c69f</task-id>\n<tool-use-id>toolu_01VJP734j9pKz4vxdNXJzsR2</tool-use-id>\n<output-file>/tmp/claude-1000/-home-koukyosyumei-Dev-h5i/87ca6763-d9b3-49d9-aa03-be75baf54218/tasks/a2517b77a93d6c69f.output</output-file>\n<status>completed</status>\n<summary>Agent \"Find prompt scoring code\" came to rest</summary>\n<note>A task-notification fires each time this agent comes to rest with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>\n<result>Perfect! Now I have a comprehensive understanding. Let me create the final report:\n\n## Summary: Prompt Maturity Score Code Analysis\n\nI've completed a thorough search of the h5i repository for all prompt scoring code. Here's my comprehensive findings:\n\n### Main File\n\n**File Path:** `/home/koukyosyumei/Dev/h5i/src/prompt_score.rs` (1372 lines)\n\n### Entry Points (When Scoring Happens)\n\n**Per-Prompt Scoring (hot path):**\n- **File:** `/home/koukyosyumei/Dev/h5i/src/pr.rs:2477` — Called once per AI commit when rendering individual commit details\n  - Function: `crate::prompt_score::score_prompt(&amp;ai.prompt)`\n  - Usage: Scores single prompt for display in PR details\n\n**Branch-Level Scoring (hot path at PR generation):**\n- **File:** `/home/koukyosyumei/Dev/h5i/src/pr.rs:784` — Called once per PR branch\n  - Function: `crate::prompt_score::score_branch(prompts, a.ai_count)`\n  - Usage: Aggregates all AI-commit prompts on a branch into a single score with coverage metrics\n\nThese are called during `h5i share pr post` execution, so the frequency depends on PR share frequency.\n\n---\n\n### Core Scoring Algorithm\n\n**Main Entry Point:** `/home/koukyosyumei/Dev/h5i/src/prompt_score.rs:285-338` — `score_prompt(prompt: &amp;str)`\n\n**Steps:**\n\n1. **Feature Extraction** (lines 456-498): `Features::extract(text)`\n   - Masks code/paths/URLs so prose metrics aren't corrupted\n   - Tokenizes into prose words (lowercased)\n   - Counts syllables, sentences, structural elements\n   - Extracts ~20 features (code_refs, quoted, numbers, action_verbs, weak_words, context_markers, grounding_refs, constraints, output_shape, verification, edge_cases, safety, scope, etc.)\n\n2. **Seven Sub-Signal Computation** (lines 500-565): `breakdown()`\n   - **Specificity** (24% weight) — code references + action verbs + quoted text + numbers − weak word density\n   - **Control** (24% weight) — constraints, verification, output shape, edge cases, scope, safety keywords\n   - **Context** (18% weight) — context markers + grounding references\n   - **Structure** (10% weight) — bullets, numbered lists, headings, code fences, multi-sentence measure\n   - **Diversity** (10% weight) — adaptive MATTR (Moving-Average Type-Token Ratio)\n   - **Clarity** (8% weight) — trapezoid band on Flesch Reading Ease &amp; Flesch-Kincaid Grade Level\n   - **Adequacy** (6% weight) — length sweet-spot curve (0 &lt; 5 words, ramps to 1.0 by 40 words, plateaus to 700, tapers to 0.6 at 1400+)\n\n3. **Weighted Combination** (lines 301-308):\n   - Multiplies each sub-signal by its weight, sums to 0–100 score\n\n4. **Anti-Gaming Guards** (lines 310-327):\n   - Repetition penalty (line 313): Fraction of duplicate bigrams multiplies score (0.6–1.0 range)\n   - Balance gates (line 316): Hard caps if control &lt; 0.35 or context &lt; 0.35 (unless tactical), specificity &lt; 0.45 caps at 79\n   - Hard length caps: &lt; 8 words → max 20, &lt; 15 words → max 45, &gt; 1200 unstructured → max 75\n\n5. **Maturity Level &amp; Flags** (lines 332-333):\n   - Maps 0–100 score to `MaturityLevel::Nascent|Developing|Proficient|Advanced|Exemplary`\n   - Diagnostic flags (up to 2): `TooShort|Vague|WeakContext|WeakVerification|Repetitive|HardToScan`\n\n---\n\n### Performance-Sensitive Parts (Identified)\n\n#### **1. String Masking (lines 671-720)** — Per-Prompt\n\n- **`mask_code()`** (lines 671-701): Iterates over `split_whitespace()` and applies `token_is_code()` heuristic\n- **`strip_fenced()`** (lines 705-720): Scans all lines; toggles fence state\n- **`token_is_code()`** (lines 725-737): Multiple pattern checks (backticks, URLs, `is_code_like()`)\n- **Issue:** Multiple passes + allocations\n  - `without_fences` string allocated\n  - `out` string built incrementally  \n  - Nested char iteration (trailing punctuation collection)\n\n#### **2. Tokenization (lines 741-755)** — Per-Prompt\n\n- **`tokenize_words()`**: Iterates every char, builds Strings individually via `to_ascii_lowercase()`\n- Stores all words as `Vec&lt;String&gt;` (allocations)\n- Used by: syllable counting, diversity measures, prose-token features\n\n#### **3. Syllable Counting (lines 781-805)** — Per-Word, Repeated\n\n- **`count_syllables(word)`** (lines 781-805): Called for every word in prose_tokens (lines 462–463)\n  - Line 462: `sum()` over all words\n  - Line 463: Again via filter for polysyllables ≥3\n  - **Double iteration** of the same data\n- Contains char iteration, vowel matching, silent-e logic\n\n#### **4. Lexical Diversity (lines 922-960)** — Per-Prompt, Complex\n\n- **`mattr(words, window)`** (lines 946-960): **O(n × window) complexity**\n  - Lines 953-958: Nested loop `for start in 0..=(n - window)`\n  - Each window creates a new `HashSet&lt;&amp;str&gt;` and iterates it (line 955)\n  - **Repeated HashSet creation** — no caching/reuse\n  - For a 200-word prompt with window=40: ~160 × 40 = 6400+ set operations\n\n- **`mtld()` &amp; `mtld_pass()`** (lines 965-1005): Forward + backward pass\n  - Iterates tokens twice (lines 969-970)\n  - Maintains HashSet with insert/clear cycles\n  - Used only for display (`mtld` is \"exposed for external callers / reporting\"), not composite\n\n#### **5. Lexicon Hits (lines 900-913)** — Per-Prompt, High-Frequency\n\nCalled **11 times per prompt** (lines 477–486) for each lexicon:\n```\naction_verbs, weak_words, context_markers, grounding_refs, \nconstraints, output_shape, verification, edge_cases, safety, scope\n```\n\n- **`lexicon_hits(lower, word_set, lex)`** (lines 900-913):\n  - Line 903-904: For **multi-word entries**, calls `lower.matches(entry).count()`\n    - Substring search for each entry (e.g., \"as possible\", \"and so on\", \"or something\")\n  - Line 906-909: For **single-word entries**, splits on non-alphanumeric + calls filter\n    - **Repeated split** of entire lowercase text for each lexicon call (11 times)\n  - **Issue:** 11 full-text lowercasing + 11 × (num_entries) substring/split operations\n\n#### **6. Repetition Factor (lines 1010-1029)** — Per-Prompt\n\n- **`repetition_factor(words)`** (lines 1010-1029):\n  - Line 1017: `words.windows(2)` — creates bigram sliding window\n  - Line 1020: `HashSet::insert()` for deduplication check\n  - Lines 1021: Track repeats\n  - Cost: ~O(n) with HashSet ops, but HashSet allocations/rebuilds\n\n#### **7. Code-Like Detection (lines 824-857)** — Per-Token\n\n- **`is_code_like(t)`** (lines 824-857): Called for every token in `count_code_refs()` (line 815)\n- Checks: paths, func calls, file extensions, snake_case, CamelCase\n- Involves: `contains()`, `rfind()`, `chars()` iteration, nested char inspection\n- **Called in two contexts:**\n  - Via `mask_code()` during feature extraction\n  - Via `count_code_refs()` (line 471) — **separate full-text iteration**\n\n#### **8. Branch Aggregation (lines 356-405)** — Per-Branch\n\n- Line 356-360: Iterates all prompts, calls `score_prompt()` per each (nested calls to all above)\n- Lines 371-390: **Multiple passes** over `scored` vector:\n  - 7 passes for `wmean_b()` calls (one per breakdown signal)\n  - 1 pass for `wmean()` score\n  - 1 pass for flags aggregation\n  - **Repeated weight computation** (line 368) per pass\n- Line 397: HashMap for flag tallying + sort (line 404)\n\n---\n\n### Specific Code Sections (with Line Numbers)\n\n| Operation | File:Lines | Complexity | Frequency | Comment |\n|-----------|-----------|-----------|-----------|---------|\n| **Code masking** | `prompt_score.rs:671–720` | O(n × m) | Per-prompt | Multiple passes, string allocations, nested char iteration |\n| **Tokenization** | `prompt_score.rs:741–755` | O(n) | Per-prompt | Vec allocation, char-by-char lowercase |\n| **Syllable counting** | `prompt_score.rs:462–463, 781–805` | O(n) | Per-prompt | **Called twice** (sum + filter) over same tokens |\n| **MATTR diversity** | `prompt_score.rs:946–960` | **O(n × window)** | Per-prompt | Nested loop, repeated HashSet creation (160+ sets for typical prompt) |\n| **Lexicon hits (11×)** | `prompt_score.rs:900–913, 477–486` | O(11 × n) | Per-prompt | **11 full-text lowercased lexicon scans**, 11 text.split() calls, substring matches |\n| **Repetition factor** | `prompt_score.rs:1010–1029` | O(n) | Per-prompt | Bigram HashSet tracking, linear scan |\n| **Branch roll-up** | `prompt_score.rs:351–417` | O(p × m + 7p) | Per-PR | Score all prompts, then 7 aggregation passes over scored list |\n| **Per-commit render** | `pr.rs:2477` | O(m) | Per-commit | Single `score_prompt()` call, not batched |\n\n**Legend:** n = prompt length, m = lexicon size, p = number of prompts, window = MATTR window (typically 10–40)\n\n---\n\n### Optimization Opportunities\n\n**High Impact (Big-O improvements):**\n1. **Cache `text.to_ascii_lowercase()` result** — called once at start, reused for all 11 lexicon_hits calls (saves 10 redundant lowercasing passes)\n2. **Pre-compile lexicon hits** — Instead of 11 separate `lexicon_hits()` calls with substring searches, build a single pass over prose tokens + one pass for multi-word entries\n3. **Collapse syllable counting passes** — Count during tokenization, not in two separate passes (lines 462–463)\n4. **Optimize MATTR** — Replace `words.windows()` + repeated HashSet creation with a single-pass sliding-window type counter (O(n) instead of O(n × window))\n5. **Cache weight computation** — `wmean` closures call `weight(s)` 9 times (7 sub-signals + 1 score + 1 aggregation) per prompt — compute once, store\n\n**Medium Impact (Constant-factor):**\n6. **Avoid double tokenization** — `tokenize_words()` on masked text is reused, but masking itself is expensive; consider lighter tokenization for diversity only\n7. **Batch flag aggregation** — Move flags collection into the per-prompt loop (line 398–401) instead of iterating scored vector again\n\n**Integration Hot Paths:**\n- **Per-commit scoring** (`pr.rs:2477`): Isolated, no batching opportunity\n- **Branch scoring** (`pr.rs:784`): Batch-friendly but currently scores sequentially with full feature extraction per prompt\n- **No caching** between calls — scoring is deterministic (good for reproducibility) but no memoization if same prompts appear\n\n---\n\n### Design Decisions (Locked by Codex)\n\nPer the module header (lines 26–73), the scoring algorithm is \"locked with the codex agent over i5h\" with specific design contracts:\n- Fully offline, deterministic, reproducible in CI/hooks\n- No regex compilation or LLM calls\n- Anti-gaming guards prevent keyword stuffing\n- Length-weighted branch aggregation (not concatenation) prevents weak prompts from pooling\n\nThese constraints rule out caching across PR invocations but allow internal optimization within a single `score_prompt()` or `score_branch()` call.</result>\n<usage><subagent_tokens>46699</subagent_tokens><tool_uses>32</tool_uses><duration_ms>88941</duration_ms></usage>\n</task-notification>\n\nyes, let's do both. Also I guess we need comprehensive tests\n\nYou should use -j4 instead of -j1. You should also update the memory to use -j4\n\nplease commit it","agent_id":"claude-code","usage":null},"test_metrics":{"test_suite_hash":"2cc912893dd96e22b20a9b52f3ad1b3d0f008c6b118aa009bdc369310c4a0c69","passed":0,"failed":0,"skipped":0,"total":0,"duration_secs":0.0,"coverage":0.0,"tool":"marker-scan","exit_code":null,"summary":"marker block detected in h5i.1"},"ast_hashes":null,"timestamp":"2026-06-18T14:35:58.468251032Z"}